{
  "$schema": "https://turborepo.org/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["ui/.next/**", "backend/dist/**"]
    },
    "start": {
      "dependsOn": ["build"],
      "cache": false
    }
  }
}
